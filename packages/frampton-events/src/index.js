import {
  addListener,
  removeListener
} from 'frampton-events/event_dispatcher';

import getPosition from 'frampton-events/get_position';

export {
  addListener,
  removeListener,
  getPosition
};